module.exports = function flexboxgrid(options) {
  options = options || {}
  var columns = options.columns || 12
  var padding = options.padding || '2rem'
  var gutters = options.gutters || padding * 0.5
  var breakPoints = options.breakPoints || {xs:30,sm:48,md:64,lg:75}


}
